{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug GC (CodeLLDB)",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/gc_test",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "build garbage collector"
        }
    ]
}